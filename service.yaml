apiVersion: v1
kind: Service
metadata:
  name: contact-service
spec:
  type: NodePort
  selector:
    app: contact-system
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007